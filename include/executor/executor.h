#pragma once

/* TODO: здесь лежит исполнитель запросов
         (?) по идее должен принимать на вход строку запрос и возвращать таблицу
             (либо ошибку, я еще не решил)

         Error Exec(const std:string& query);
*/
